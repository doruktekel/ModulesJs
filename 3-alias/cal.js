////  Default export for default as

// const sum = (a, b) => {
//   return a + b;
// };

// export default sum;

//////  For Alias Examples

const sum = (a, b) => {
  return a + b;
};

const sub = (a, b) => {
  return a + b;
};

export { sum, sub };
